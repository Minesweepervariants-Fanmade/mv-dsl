# mv-dsl

Declarative rule DSL and CP-SAT solver for **14 Minesweeper Variants** (1 & 2).

覆盖官方两代游戏的全部规则，支持规则组合（如 `2E&`、`2L&`）与未来扩展（如雷线索）。

## 设计要点

- **声明式外壳**：谜题是数据（board / rules / grid / sideboards / cell 多角色）
- **函数式内核**：规则是组合子的组合，约束由编译器生成
- **六类组合子**：`Region` / `Weight` / `Aggregate` / `Relation` / `Global` / `Sideboard`
- **统一管道**：$\text{clue} = \mathrm{Relation}(\mathrm{Aggregate}(\mathrm{Weight}(\mathrm{Region}(i,j))))$
- **组合规则 = 管道复合**，不为任何组合写专门代码
- **后端无关 IR**：CP-SAT 为主后端，Z3 / csugar 可选

架构与完整计划见 [PROJECT.md](./PROJECT.md)。

## 状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| 一 | IR + CP-SAT 后端 | **完成** |
| 二 | 官方格式导入器（两代）+ mv1 全部 15 条线索规则 | **完成** |
| 三 | 全局规则 + mv2 规则 | 进行中（mv2 规则未开始） |
| 四 | 唯一性验证 / 全解枚举（已有）/ 难度评估 | 部分完成（难度评估非优先） |
| 五 | Server 对接 | 待定 |

### 已验证成果

| 验证项 | 结果 |
|---|---|
| mv1 关卡导入 | 146,291 关全量通过，179 种规则组合 |
| mv2 关卡导入 | 323,279 关全量通过，417 种规则组合，副板四类正确切分 |
| 线索求值器 vs legacy 基准 | **1,823,432 格、15 条规则，100% 一致** |
| mv1 官方答案盘满足约束 | 采样 1,500 关，**全部可编译、100% 通过** |
| 端到端求解（CP-SAT） | 40/40 可解，平均 19.8 ms/关（含唯一性求解） |
| mv2 规则覆盖 | 采样 800 关全部跳过（规则未实现），为下一阶段基线 |

> **领域事实**：14mv 是交互式游戏而非纸笔谜题——未揭示格（`q`）的线索值不在官方文件中，
> 玩家需点击揭示逐步获取信息。因此**官方关卡不保证静态唯一解**，
> 「求解结果 == 答案盘」不是有效断言；正确性由「答案盘满足全部约束」保证。
> 唯一解是**出题器**（动态挖洞）的要求，不是官方关卡的性质。

## 开发

```powershell
uv sync
uv run pytest
```

## 许可证

Apache-2.0
